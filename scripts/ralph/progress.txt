# Ralph Progress Log

Started: 2026-06-20

## Codebase Patterns

- The current implementation is a dependency-free Node ESM server. `npm test` and `npm run check` are the mandatory local quality gates.
- `LABS_DEMO_MODE` and Vercel's ephemeral `/tmp` database are demo-only boundaries; production code must fail closed when essential configuration is missing.

## 2026-06-20 — US-001 Document production runtime configuration

- Implemented an explicit runtime configuration contract in `src/runtime-config.mjs`; production validates OIDC, PostgreSQL, tenant, approved-artifact, notification, and integration provider configuration using non-secret issue codes.
- Removed Vercel as an implicit demo-mode switch. Demo SQLite and seeded identities now require `LABS_DEMO_MODE=true`; non-demo runtime avoids SQLite/demo fallback and remains fail-closed until the planned production adapters are available.
- Added `docs/runtime-configuration.md`, README deployment guidance, focused configuration tests, and syntax coverage for the new module. Files committed in `22491ea` with `feat: US-001 - Document production runtime configuration`.
- Verification: `npm test` (13 passing) and `npm run check` passed. Vercel CLI authentication and project linkage are available, but `npx vercel env ls preview` reported `No Environment Variables found`; no preview was created because the required explicit runtime configuration is absent.
- Reusable learning: do not infer demo mode from a deployment platform. Treat demo mode as an explicit operator decision, and expose stable missing-configuration codes rather than environment values.

## 2026-06-20 — US-002 Add OIDC identity-provider interface

- Added `src/identity-provider.mjs`, a server-side identity contract that returns immutable verified subject, group, organization, and session-expiry metadata. The explicit demo/test adapter is seeded from active demo users only; the production adapter fails closed and never trusts caller-supplied identity headers.
- Routed every API request through the identity boundary before workflow access, and added focused identity-provider tests plus syntax coverage. Updated deployment guidance to state that production requires a verified token adapter rather than an identity proxy header.
- Verification: `npm test` (17 passing), `npm run check`, and a manual explicit-demo session request passed. Vercel CLI authentication and linkage are available, but `npx vercel env ls preview` returned `No Environment Variables found`; no preview was created because the required production configuration is absent.
- Files changed: `src/identity-provider.mjs`, `server.mjs`, `src/labs-store.mjs`, `test/identity-provider.test.mjs`, `package.json`, `README.md`, and `docs/runtime-configuration.md`.
- Reusable learning: make the identity boundary return a complete verified identity object, and keep header-derived claims out of production until signature and claim verification are in place.

## 2026-06-20 — US-003 Verify OIDC bearer tokens

- Implemented `OidcIdentityProvider` with server-side bearer-token verification against explicitly configured HTTPS issuer, audience, tenant claim, and JWKS URL. The verifier accepts only `RS256` and `ES256` signing keys from the configured JWKS, caches keys briefly, refreshes once on an unknown key ID, and never logs token content.
- Routed API identity resolution asynchronously through the verifier. Invalid token syntax, signatures, expiry, issuer, and audience return non-sensitive structured authentication errors; unavailable JWKS returns a non-sensitive service-unavailable error.
- Added the `LABS_OIDC_JWKS_URL` runtime contract, HTTPS configuration validation, operator documentation, and focused cryptographic verification tests. Files changed: `src/identity-provider.mjs`, `src/runtime-config.mjs`, `server.mjs`, `test/identity-provider.test.mjs`, `test/runtime-config.test.mjs`, `README.md`, and `docs/runtime-configuration.md`.
- Verification: `npm test` (21 passing) and `npm run check` passed. Vercel CLI authentication and project linkage are available, but `npx vercel env ls preview` reported `No Environment Variables found`; no preview was created because the required production configuration is absent.
- Reusable learning: mutate a significant decoded signature byte when testing JWS signature rejection; a change only to unused base64url padding bits may leave the decoded signature unchanged.

## 2026-06-20 — US-004 Map verified groups to application roles

- Added a strict `LABS_GROUP_ROLE_MAPPING` production contract that maps every application role to approved verified OIDC groups. Mappings must be complete and groups cannot be shared between roles; identities with no match or more than one match are denied.
- Routed request actors through the verified-group role resolver. The persisted demo user record supplies only display metadata; its role no longer determines request authorization. Demo identity switching remains available only under explicit `LABS_DEMO_MODE=true` and production ignores its header.
- Added mapping and configuration tests, syntax coverage, and operator documentation. Files changed: `src/role-mapping.mjs`, `server.mjs`, `src/runtime-config.mjs`, `test/role-mapping.test.mjs`, `test/runtime-config.test.mjs`, `package.json`, `README.md`, and `docs/runtime-configuration.md`.
- Verification: `npm test` (26 passing) and `npm run check` passed. Vercel CLI credentials and linkage are available, but `npx vercel env ls preview` reported `No Environment Variables found`; no preview was created because the required production configuration contract is absent.
- Reusable learning: resolve a single server-owned role from verified claims and reject overlaps explicitly; selecting a more privileged role from a multi-group identity silently converts group configuration drift into privilege escalation.

## 2026-06-20 — US-005 Add secure session and CSRF protection

- Added `src/request-security.mjs`, which preserves verified OIDC bearer-token authentication as the only current production transport and requires an exact same-origin, constant-time matched CSRF token for any future cookie-authenticated mutation. It fails closed when a cookie transport lacks an HTTPS application origin.
- Applied one strict header set to API, error, and static responses: the existing self-only CSP remains intact, with HSTS, no-sniff, referrer, permissions, opener, and resource policies added. Cookie sessions are not enabled until a server-side session adapter is available; identity headers remain demo-only.
- Added targeted CSRF acceptance/rejection and response-header tests. Files changed: `src/request-security.mjs`, `src/identity-provider.mjs`, `server.mjs`, `test/request-security.test.mjs`, `package.json`, and `README.md`.
- Verification: `npm test` (30 passing) and `npm run check` passed. Vercel CLI authentication and project linkage are available, but `npx vercel env ls preview` reported `No Environment Variables found`; no preview was created because required production runtime configuration is absent.
- Reusable learning: treat CSRF as a property of cookie credentials, not a blanket replacement for bearer-token verification; apply the same CSP and security headers to JSON error responses as to static assets.

## 2026-06-20 — US-006 Define production storage port

- Added `src/storage-port.mjs` with an explicit persistence contract for users, projects, decisions, audit events, and transactions. `WorkflowService` now owns lifecycle, authorization, gate, approval, handoff, and audit orchestration without importing SQLite.
- Refactored `LabsStore` into a compatibility facade over `WorkflowService` and the demo-only `SqliteLabsStorage` adapter. Existing workflow tests therefore continue to exercise the SQLite demo/test adapter through the same public API.
- Added storage-port contract coverage and syntax checks. Files changed: `src/storage-port.mjs`, `src/workflow-service.mjs`, `src/labs-store.mjs`, `test/storage-port.test.mjs`, and `package.json`.
- Verification: `npm test` (32 passing) and `npm run check` passed. Vercel CLI authentication and linkage are available, but `npx vercel env ls preview` reported `No Environment Variables found`; no preview was created because the required explicit production runtime configuration is absent.
- Reusable learning: model the persistence boundary with domain operations and transaction ownership, so storage adapters can change without weakening server-owned workflow policy or durable audit behavior.

## 2026-06-20 — US-007 Add PostgreSQL migration framework

- Added a `pg`-backed `npm run migrate` command and a migration runner that takes an advisory lock, applies each ordered SQL migration transactionally, and records immutable version/checksum history in `schema_migrations`. The command requires `LABS_DATABASE_URL`, rejects demo mode, and does not log connection values.
- Added PostgreSQL core tables for cycles, projects, gates, evidence, reviews, decisions, approvals, handoffs, and audit events. Audit rows are database-enforced append-only; user references are opaque directory subjects rather than directory-profile content. Tenant isolation remains intentionally deferred to the next scoped story, and the production runtime stays fail-closed in the interim.
- Added migration runner tests for ordering, repeat runs, checksum tampering, failures/rollback, and ambiguous sequence numbers; documented the migration procedure and production database requirement. Files changed: `migrations/`, `src/migration-runner.mjs`, `scripts/migrate.mjs`, `test/migration-runner.test.mjs`, `package.json`, `package-lock.json`, `README.md`, and `docs/runtime-configuration.md`.
- Verification: `npm test` (36 passing) and `npm run check` passed. Vercel CLI authentication and linkage are available, but `npx vercel env ls preview` reported `No Environment Variables found`; no preview was created because the required production runtime configuration and company-approved PostgreSQL URL are absent.
- Reusable learning: track SQL migrations by checksum and run them behind a database-level lock, so deployment retries are safe and edited migration history fails before it can corrupt an authoritative schema.

## 2026-06-20 — US-008 Add tenant-scoped production schema

- Added `003_organization_tenant_scope.sql`, which adds opaque organizations and non-null organization scope to all production workflow records, including audit events. Composite foreign keys require every parent, user reference, and workflow child to remain in the same organization; tenant-prefixed indexes support scoped queries.
- The migration stops before changing a populated pre-tenant schema rather than infer a tenant or copy demo data. Documented the clean-database production path and the requirement for an approved, separately reviewed migration plan if existing authoritative rows ever require assignment.
- Added schema-level migration assertions for organization columns, tenant foreign keys, audit scoping/indexing, and the no-demo-copy safeguard. Files committed in `76fb9eb` with `feat: US-008 - Add tenant-scoped production schema`.
- Verification: `npm test` (37 passing) and `npm run check` passed. Vercel CLI authentication and linkage are available, but `npx vercel env ls preview` returned `No Environment Variables found`; no preview was created because required production runtime configuration is absent.
- Reusable learning: enforce tenant isolation in relational keys as well as future query policy, and reject unassignable legacy data rather than silently placing it into a catch-all tenant.

## 2026-06-20 — US-009 Implement PostgreSQL read adapter

- Added `src/postgres-read-adapter.mjs`, a read-only PostgreSQL adapter for tenant-scoped verified-user, portfolio, evidence, review, decision, approval, handoff, and audit reads. All query values are parameterized, and database failures return the structured `DATABASE_UNAVAILABLE` error without a SQLite fallback.
- Routed configured production `GET /api/v1/projects` and `GET /api/v1/audit-events` requests through the adapter after exact verified-tenant matching. Production mutations return `MUTATION_ADAPTER_UNAVAILABLE` until US-010 provides atomic workflow and audit transactions.
- Added query-mock coverage for tenant bindings, serialized project reads, audit access, and non-secret database errors; updated runtime documentation and syntax checks. Files changed: `src/postgres-read-adapter.mjs`, `server.mjs`, `src/runtime-config.mjs`, `test/postgres-read-adapter.test.mjs`, `test/runtime-config.test.mjs`, `package.json`, `README.md`, and `docs/runtime-configuration.md`.
- Verification: `npm test` (39 passing) and `npm run check` passed. Vercel CLI authentication and linkage are available, but `npx vercel env ls preview` returned `No Environment Variables found`; no preview was created because the required production configuration is absent.
- Reusable learning: keep read access tenant-scoped in every SQL query and hold mutations fail-closed until the business write and its audit event can share one database transaction.

## 2026-06-20 — US-010 Implement PostgreSQL mutation transactions

- Added `src/postgres-workflow-adapter.mjs`, which handles tenant-scoped project, gate, evidence, review, decision, approval, and handoff mutations on a single PostgreSQL client transaction. Each public workflow mutation appends its durable audit event before commit; any write or audit failure rolls the transaction back and returns a non-sensitive database error.
- Routed configured production API writes through the PostgreSQL workflow adapter, retaining SQLite solely for explicit demo mode. Production readiness now succeeds only when its required configuration is present and the database health check succeeds.
- Added transaction and rollback coverage, updated syntax coverage and runtime documentation. Files committed in `d4c175c` with `feat: US-010 - Implement PostgreSQL mutation transactions`.
- Verification: `npm test` (41 passing) and `npm run check` passed. Vercel CLI authentication and project linkage are available, but `npx vercel env ls preview` returned `No Environment Variables found`; no preview was created because required OIDC, database, tenant, approved-artifact, notification, and integration configuration is absent.
- Reusable learning: use a checked-out PostgreSQL client—not a pool-level query helper—for transaction scope, so the workflow state change and append-only audit insert can be committed or rolled back as one unit.

## 2026-06-23 — US-011 Require production database readiness

- Added a production-only PostgreSQL readiness probe that checks connectivity and compares the full tracked migration checksum set to `schema_migrations`. Production health, readiness, and API operations fail closed when the database is unavailable, the schema migration table is absent, history is altered, or migrations are pending; demo mode remains explicitly non-ready.
- `/readyz` exposes only non-secret connectivity, migration-state, pending-version, and stable issue-code metadata. Added focused readiness coverage for current, pending, missing, altered, unavailable, and client-cleanup paths.
- Verification: `npm test` (44 passing) and `npm run check` passed. `npx vercel env ls preview` reported `No Environment Variables found` for the linked project, so no preview was created: the production OIDC, PostgreSQL, tenant, approved-artifact, and integration configuration contracts are absent.
- Files changed: `src/database-readiness.mjs`, `server.mjs`, `test/database-readiness.test.mjs`, `package.json`, `docs/runtime-configuration.md`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: readiness must validate both reachability and migration provenance; a successful `SELECT 1` cannot prove that the service can safely operate against its authoritative schema.

## 2026-06-23 — US-012 Add soft-delete lifecycle controls

- Added tenant-safe soft-delete metadata and active-record indexes for cycles, projects, evidence entries, decisions, and handoffs; audit events remain append-only and are not deletable. Normal project reads exclude deleted projects.
- Added administrator-only project delete/restore endpoints and workflow methods. Reasons are a fixed metadata enum rather than free-form content, and every delete/restore shares its transaction with a durable audit event.
- Verification: `npm test` (46 passing) and `npm run check` passed. Vercel preview remains blocked because the linked preview environment has no required configuration variables.
- Files changed: `migrations/004_soft_delete_lifecycle.sql`, `src/labs-store.mjs`, `src/storage-port.mjs`, `src/workflow-service.mjs`, `src/postgres-read-adapter.mjs`, `src/postgres-workflow-adapter.mjs`, `server.mjs`, `test/migration-runner.test.mjs`, `test/workflow.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: represent deletion reasons as controlled metadata; it preserves an audit explanation without creating another free-text path for sensitive content.

## 2026-06-23 — US-013 Add retention-policy enforcement metadata

- Added server-owned `program_record` retention metadata and a seven-year minimum timestamp to final decisions and every audit event, with production migration indexes and demo-adapter parity.
- Documented the retention floor and guarded ordinary project soft deletion whenever a linked finalized decision has not reached its retention date.
- Verification: `npm test` (48 passing) and `npm run check` passed. Vercel preview remains blocked by missing required preview-environment configuration.
- Files changed: `migrations/005_retention_policy_metadata.sql`, `src/retention-policy.mjs`, `src/labs-store.mjs`, `src/storage-port.mjs`, `src/workflow-service.mjs`, `src/postgres-workflow-adapter.mjs`, `test/migration-runner.test.mjs`, `test/workflow.test.mjs`, `docs/runtime-configuration.md`, `package.json`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: retention must be applied by the server at the moment authoritative records are created or finalized; client-provided expiry values are not a trustworthy compliance control.

## 2026-06-23 — US-014 Add audit-log integrity verification

- Added tenant-scoped audit sequence, prior-hash, and SHA-256 event-hash metadata. PostgreSQL serializes chain appends inside its workflow transaction; the demo adapter follows the same chain model.
- Added administrator-protected `GET /api/v1/audit-events/verify`, migration safeguards for historical chain baselining, operational documentation, and altered-record detection tests.
- Verification: `npm test` (50 passing) and `npm run check` passed. Vercel preview remains blocked by missing required preview-environment configuration.
- Files changed: `migrations/006_audit_integrity_chain.sql`, `src/audit-integrity.mjs`, `src/labs-store.mjs`, `src/storage-port.mjs`, `src/workflow-service.mjs`, `src/postgres-workflow-adapter.mjs`, `server.mjs`, `test/migration-runner.test.mjs`, `test/workflow.test.mjs`, `docs/runtime-configuration.md`, `package.json`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: audit immutability needs independent integrity verification; append-only database permissions alone cannot prove that historical rows have not been altered out of band.

## 2026-06-23 — US-015 Document backup and restore operations

- Added a managed-PostgreSQL backup and restore runbook with RPO/RTO targets, quarterly restore-test evidence, ownership/escalation boundaries, readiness checks, and mandatory audit-chain verification after restoration.
- Verification: `npm test` (50 passing) and `npm run check` passed. Vercel preview remains blocked by missing required preview-environment configuration.
- Files changed: `docs/backup-and-restore-runbook.md`, `README.md`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: a restore runbook must separate the provider-owned recovery mechanics from the application-owned integrity and authorization checks, so neither team assumes the other verified end-to-end recoverability.

## 2026-07-02 — US-016 Add intake draft persistence

- Added server-owned intake draft persistence with explicit `Draft` status, owner and draft-collaborator access checks, create/read/update API endpoints, and durable audit events for draft creation and updates.
- Kept drafts separate from submitted projects through a dedicated SQLite demo table and tenant-scoped PostgreSQL `intake_drafts` migration, so drafts do not enter portfolio reads, selection queues, or decision workflow state.
- Verification: `npm test` (53 passing) and `npm run check` passed. `npx vercel env ls preview` reported `No Environment Variables found` for the linked Vercel project, so no preview was created because required production runtime configuration is absent.
- Files changed: `migrations/007_intake_draft_persistence.sql`, `server.mjs`, `src/labs-store.mjs`, `src/postgres-read-adapter.mjs`, `src/postgres-workflow-adapter.mjs`, `src/storage-port.mjs`, `src/workflow-service.mjs`, `test/migration-runner.test.mjs`, `test/postgres-read-adapter.test.mjs`, `test/postgres-workflow-adapter.test.mjs`, `test/storage-port.test.mjs`, `test/workflow.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: keep incomplete intake content in an access-scoped draft table until submission; overloading the authoritative project table would either require placeholder governance data or risk exposing drafts through portfolio queries.

## 2026-07-02 — US-017 Build intake draft UI

- Added intake save-draft and resume-draft controls backed by the server draft endpoints. The draft save path bypasses required submit validation so incomplete intakes can be saved, while final submission still uses the governed intake submit endpoint.
- Added an accessible draft status region for save/load/resume failures and hid draft lists, save controls, and previously loaded draft content when the active role is not authorized for draft access.
- Added static frontend regression coverage for the no-build UI contract and included the test in `npm run check` syntax coverage.
- Verification: `npm test` (56 passing) and `npm run check` passed. A local demo-mode HTTP verification confirmed the static controls were served, `submitter-1` could create/list/resume a draft, and `receiving-owner` received `403 FORBIDDEN` for the same draft. In-app browser verification was blocked because the Browser plugin reported no available browser backends (`agent.browsers.list()` returned `[]`).
- Vercel preview: `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `app.js`, `index.html`, `styles.css`, `package.json`, `test/intake-draft-ui.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: in the static prototype, keep draft visibility decisions aligned with the server role contract and clear loaded draft state when the verified role changes to one that cannot access draft endpoints.

## 2026-07-02 — US-018 Add intake collaborators

- Added explicit intake draft collaborator records with draft-level `edit` permission metadata in SQLite demo storage and PostgreSQL migrations, while preserving the metadata-only draft boundary.
- Added owner-only collaborator add/remove workflow methods and API routes. Collaborators can edit draft content but cannot change collaborators, change draft ownership, or submit by mutating draft status.
- Audited collaborator add/remove operations as durable draft-collaborator events, separate from ordinary draft content updates.
- Verification: `npm test` (57 passing) and `npm run check` passed. `npx vercel env ls preview` reported `No Environment Variables found` for the linked Vercel project, so no preview was created because required production runtime configuration is absent.
- Files changed: `migrations/008_intake_draft_collaborators.sql`, `server.mjs`, `src/labs-store.mjs`, `src/postgres-read-adapter.mjs`, `src/postgres-workflow-adapter.mjs`, `src/storage-port.mjs`, `src/workflow-service.mjs`, `test/migration-runner.test.mjs`, `test/postgres-read-adapter.test.mjs`, `test/postgres-workflow-adapter.test.mjs`, `test/storage-port.test.mjs`, `test/workflow.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: model draft collaboration as first-class permission metadata rather than a mutable ID array; it gives the workflow a durable surface for authorization and audit without opening a new path for intake submission.

## 2026-07-02 — US-019 Support intake submission and withdrawal

- Added explicit draft submission and intake withdrawal workflow methods with server-owned state guards. Draft submission is owner-only, rejects incomplete selection data, creates the submitted project and draft status update in one audited transaction, and prevents further draft edits after submission.
- Added owner-only withdrawal for submitted or triaged intakes before selection. Withdrawal uses the existing controlled soft-delete metadata with reason `withdrawn`, keeping withdrawn intakes out of normal project reads without introducing a non-spec lifecycle stage.
- Added API routes for `POST /api/v1/intake-drafts/{id}/submit` and `POST /api/v1/intakes/{id}/withdraw`, and updated the draft UI submit path to use the explicit submit transition when a saved draft is active.
- Verification: `npm test` (60 passing) and `npm run check` passed. `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `migrations/009_intake_submit_withdraw.sql`, `server.mjs`, `app.js`, `src/labs-store.mjs`, `src/postgres-read-adapter.mjs`, `src/postgres-workflow-adapter.mjs`, `src/storage-port.mjs`, `src/workflow-service.mjs`, `test/intake-draft-ui.test.mjs`, `test/migration-runner.test.mjs`, `test/postgres-workflow-adapter.test.mjs`, `test/storage-port.test.mjs`, `test/workflow.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: model withdrawal as an owner-governed soft delete with an audited controlled reason; this preserves lifecycle semantics and retention/audit behavior without making withdrawn intake content visible in portfolio queues.

## 2026-07-02 — US-020 Add triage comments and requests for information

- Added chronological project triage comments with author, timestamp, and controlled comment kind metadata. Comment text is available only through a dedicated intake triage endpoint rather than portfolio list reads.
- Added server-owned request-for-information state on submitted or triaged intakes. Reviewers can request information without changing the lifecycle stage; assigned intake participants and governance reviewers can access the triage thread.
- Implemented SQLite demo storage, tenant-scoped PostgreSQL migration/read/write paths, API routes, durable audit events, and focused tests for visibility, chronological ordering, RFI state, and transaction-backed writes.
- Verification: `npm test` (63 passing) and `npm run check` passed. `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `migrations/010_triage_comments_requests.sql`, `server.mjs`, `src/labs-store.mjs`, `src/postgres-read-adapter.mjs`, `src/postgres-workflow-adapter.mjs`, `src/storage-port.mjs`, `src/workflow-service.mjs`, `test/migration-runner.test.mjs`, `test/postgres-read-adapter.test.mjs`, `test/postgres-workflow-adapter.test.mjs`, `test/storage-port.test.mjs`, `test/workflow.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: keep triage discussion content off broad portfolio reads; expose only the RFI state there, and put full comment visibility behind participant/reviewer authorization.

## 2026-07-02 — US-021 Version intake revisions and comparisons

- Added immutable intake revision snapshots for submitted intakes. Initial submission records revision 1, and owner-only resubmission before selection creates the next revision while updating current project metadata.
- Added authorized revision listing and comparison behavior for intake participants and governance reviewers. Comparison responses return changed field names with before/after metadata, and broad portfolio reads still avoid revision content.
- Implemented SQLite demo storage, tenant-scoped PostgreSQL migration/read/write paths, API routes for resubmission and comparison, durable audit events, and focused workflow/adapter tests for authorization, immutability, and changed-field output.
- Verification: `npm test` (66 passing) and `npm run check` passed. `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `migrations/011_intake_revision_history.sql`, `server.mjs`, `src/labs-store.mjs`, `src/postgres-read-adapter.mjs`, `src/postgres-workflow-adapter.mjs`, `src/storage-port.mjs`, `src/workflow-service.mjs`, `test/migration-runner.test.mjs`, `test/postgres-read-adapter.test.mjs`, `test/postgres-workflow-adapter.test.mjs`, `test/storage-port.test.mjs`, `test/workflow.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: preserve immutable intake history in a separate revision table and expose comparison through authorized endpoints, so reviewer context improves without turning project list reads into a content leak.

## 2026-07-02 — US-022 Add cycle administration data model

- Added governed cycle metadata for theme, dates, capacity units, steering group members, and status in demo and PostgreSQL storage.
- Added administrator-only cycle create/update workflow methods and API endpoints. Cycle mutations validate date ranges, capacity bounds, steering group IDs, and controlled status values before writing.
- Added durable audit events for cycle creation and updates plus migration, workflow, storage-port, and PostgreSQL adapter coverage.
- Verification: `npm test` (69 passing) and `npm run check` passed. `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `migrations/012_cycle_administration.sql`, `server.mjs`, `src/labs-store.mjs`, `src/postgres-read-adapter.mjs`, `src/postgres-workflow-adapter.mjs`, `src/storage-port.mjs`, `src/workflow-service.mjs`, `test/migration-runner.test.mjs`, `test/postgres-read-adapter.test.mjs`, `test/postgres-workflow-adapter.test.mjs`, `test/storage-port.test.mjs`, `test/workflow.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: keep cycle capacity and steering membership as server-owned program metadata, not client-side labels, so later selection and governance rules can depend on an audited source.

## 2026-07-02 — US-023 Build cycle administration UI

- Added an administrator-only cycle administration view with a configured-cycle list, create/edit form, status controls, capacity input, and multi-select steering group membership.
- Added accessible validation summary behavior and status announcements for save/load failures. The Cycles nav and administration controls stay hidden for non-admin roles, and direct navigation is guarded client-side.
- Added static UI coverage for the admin-only nav, accessible validation elements, and cycle API usage.
- Verification: `npm test` (70 passing) and `npm run check` passed. In-app browser verification was blocked because the Browser plugin reported no available browser backends (`agent.browsers.list()` returned `[]`). A local demo-mode HTTP/API verification confirmed the static cycle controls were served, `admin` could list/create cycles, and `lab-lead` received `403 FORBIDDEN` when trying to create a cycle.
- Vercel preview: `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `index.html`, `app.js`, `styles.css`, `test/intake-draft-ui.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: make administrative UI discoverability follow the server role contract, and keep validation summaries focusable so failed configuration saves are usable from the keyboard.

## 2026-07-02 — US-024 Enforce cycle capacity during selection

- Added project capacity units and persisted cycle association to project reads, submitted intake snapshots, SQLite demo storage, and PostgreSQL read/write paths.
- Selection now checks the target cycle's approved capacity against already selected/incubating/decision-pending work and returns `CYCLE_CAPACITY_EXCEEDED` with cycle id, requested units, used capacity, and remaining capacity.
- Added production migration coverage and workflow tests proving capacity is consumed by the first selected project and blocks the next selection when the cycle is full.
- Verification: `npm test` (72 passing) and `npm run check` passed. `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `migrations/013_project_capacity_units.sql`, `src/labs-store.mjs`, `src/postgres-read-adapter.mjs`, `src/postgres-workflow-adapter.mjs`, `src/storage-port.mjs`, `src/workflow-service.mjs`, `test/migration-runner.test.mjs`, `test/postgres-read-adapter.test.mjs`, `test/postgres-workflow-adapter.test.mjs`, `test/storage-port.test.mjs`, `test/workflow.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: enforce cycle capacity at the server-owned selection transition; UI capacity indicators are useful context, but the transition must be the source of truth.

## 2026-07-02 — US-025 Add feature-flag service

- Added tenant-scoped feature flags with explicit defaults for intake resubmission and unfinished integration surfaces. Stored overrides merge with defaults in SQLite demo storage and PostgreSQL reads.
- Added administrator-only feature-flag list/update API methods, transaction-backed PostgreSQL writes, and durable `feature_flag_updated` audit events with before/after enabled values.
- Enforced the `intake_resubmission` flag server-side before the protected resubmission workflow runs; disabled behavior returns a structured `FEATURE_DISABLED` error.
- Verification: `npm test` (75 passing) and `npm run check` passed. `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `migrations/014_feature_flags.sql`, `package.json`, `server.mjs`, `src/feature-flags.mjs`, `src/labs-store.mjs`, `src/postgres-read-adapter.mjs`, `src/postgres-workflow-adapter.mjs`, `src/storage-port.mjs`, `src/workflow-service.mjs`, `test/migration-runner.test.mjs`, `test/postgres-read-adapter.test.mjs`, `test/postgres-workflow-adapter.test.mjs`, `test/storage-port.test.mjs`, `test/workflow.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: use feature flags as server-enforced transition guards, not UI hints; defaults should be explicit in code and tenant overrides should remain auditable metadata.

## 2026-07-02 — US-026 Add role-administration audit controls

- Added role-assignment metadata in a dedicated table separate from external group claims. Demo/test actor resolution can use active assignments, while production OIDC group mapping remains the configured authentication source of truth.
- Added administrator-only role-assignment list/update API methods with durable audit events that capture before/after role and active-state values.
- Added privilege-escalation guards that prevent administrators from granting themselves final-decision authorization roles, plus tests for unauthorized mutation, invalid roles, assignment activation/deactivation, and audit history.
- Verification: `npm test` (78 passing) and `npm run check` passed. `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `migrations/015_role_assignments.sql`, `server.mjs`, `src/labs-store.mjs`, `src/postgres-read-adapter.mjs`, `src/postgres-workflow-adapter.mjs`, `src/storage-port.mjs`, `src/workflow-service.mjs`, `test/migration-runner.test.mjs`, `test/postgres-read-adapter.test.mjs`, `test/postgres-workflow-adapter.test.mjs`, `test/storage-port.test.mjs`, `test/workflow.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: keep role administration as auditable assignment metadata and explicitly block self-grants of decision authority; authorization policy should not rely on client-visible role controls.

## 2026-07-02 — US-027 Define directory integration interface

- Added a vendor-neutral directory adapter contract for lookup and search results with active status, display identity, organization, manager, and verification timestamp metadata.
- Added a disabled adapter for absent configuration, bounded timeout behavior, sanitized provider-failure errors, and an active-person validation helper that never treats unknown, inactive, timed-out, or unavailable directory results as valid.
- Added adapter contract tests for normalization, missing configuration, provider failures, timeout behavior, inactive users, and unknown users.
- Verification: `npm test` (82 passing) and `npm run check` passed. `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `src/directory-adapter.mjs`, `test/directory-adapter.test.mjs`, `package.json`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: directory lookups must fail closed at the adapter boundary; downstream workflow should consume normalized active-person metadata, not provider-specific payloads or silent fallbacks.

## 2026-07-02 — US-028 Validate owners through directory lookup

- Wired directory validation into intake submission and resubmission for sponsor, receiving owner, metric owner, and project lead assignments. Demo storage now exposes a local directory adapter, while production workflow defaults to a disabled adapter unless an approved provider is configured.
- Added synchronous directory validation support for the demo workflow and async validation for the PostgreSQL workflow adapter. Inactive, unknown, unavailable, or timed-out directory records reject submission with structured workflow errors.
- Confirmed intake persistence continues to store only approved user reference metadata; raw directory-only fields such as email or employee number are not copied into project records.
- Verification: `npm test` (83 passing) and `npm run check` passed. `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `src/directory-adapter.mjs`, `src/labs-store.mjs`, `src/postgres-workflow-adapter.mjs`, `src/workflow-service.mjs`, `test/storage-port.test.mjs`, `test/workflow.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: perform directory validation at the workflow boundary and persist only stable user references/display metadata already allowed by the product spec.

## 2026-07-02 — US-029 Build directory-backed people picker

- Added a role-gated directory people search API for draft-capable actors and sanitized responses to active person id, display name, organization, manager id, and active state only.
- Replaced hard-coded intake person dropdown options with accessible search-backed pickers for metric owner, sponsor, receiving owner, and project lead. The controls announce loading, empty-result, and error states through the intake status region.
- Added workflow and static UI tests proving the search contract is authorized, excludes inactive people, does not expose email or employee numbers, and removes hard-coded intake owner choices.
- Verification: `npm test` (85 passing) and `npm run check` passed. In-app browser verification was blocked because the Browser plugin reported no available browser backends (`agent.browsers.list()` returned `[]`). A local demo-mode HTTP/API verification confirmed picker markup was served, submitter search returned sanitized active-person metadata, and `receiving-owner` received `403 FORBIDDEN`.
- Vercel preview: `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `server.mjs`, `src/postgres-workflow-adapter.mjs`, `src/workflow-service.mjs`, `index.html`, `app.js`, `styles.css`, `test/intake-draft-ui.test.mjs`, `test/workflow.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: keep directory search behind the same workflow authorization boundary as draft intake work, and return only normalized people metadata needed for assignment decisions.

## 2026-07-02 — US-030 Display directory organization context

- Added read-time directory assignment enrichment for metric owner, sponsor, receiving owner, and project lead without persisting raw directory payloads into project records.
- Project responses now include allowed organization, manager, active status, verification time, and stale state, plus structured warning codes for inactive, stale, or unverifiable assignments.
- Updated the project brief to show directory organization/manager context and warning codes. Inactive receiving owners are no longer counted as valid owner coverage, cannot acknowledge handoff actions from the UI, and cannot pass the server-owned selection transition.
- Verification: `npm test` (87 passing) and `npm run check` passed. `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `src/directory-context.mjs`, `src/workflow-service.mjs`, `src/postgres-workflow-adapter.mjs`, `app.js`, `styles.css`, `package.json`, `test/intake-draft-ui.test.mjs`, `test/workflow.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: treat directory context as volatile read-time metadata; stale or inactive assignments should warn and block dependent lifecycle transitions instead of being treated as durable owner truth.

## 2026-07-02 — US-031 Add delivery-kit data model and API

- Added the required delivery-kit item set for architecture, evaluation, operating model, onboarding, support, cost, monitoring, and rollback, with default `not_started` rows synthesized on reads.
- Added SQLite demo storage, tenant-scoped PostgreSQL migration/read/write paths, and REST endpoints to list, upsert, and delete delivery-kit items under each project.
- Enforced project-lead/lab-lead/admin write authorization, accountable owner validation, approved evidence links for completed items, and durable audit events for item updates and deletes.
- Verification: `npm test` (89 passing) and `npm run check` passed. `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `migrations/016_delivery_kit_items.sql`, `server.mjs`, `src/delivery-kit.mjs`, `src/labs-store.mjs`, `src/postgres-read-adapter.mjs`, `src/postgres-workflow-adapter.mjs`, `src/storage-port.mjs`, `src/workflow-service.mjs`, `package.json`, `test/migration-runner.test.mjs`, `test/postgres-read-adapter.test.mjs`, `test/postgres-workflow-adapter.test.mjs`, `test/workflow.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: keep delivery readiness as structured metadata and approved links; do not turn delivery-kit items into free-form storage for operating docs or production logs.

## 2026-07-02 — US-032 Build delivery-kit workspace

- Added a delivery-kit workspace inside the project brief that renders every required item with status, accountable owner, approved evidence link, and acceptance date.
- Added accessible per-item edit controls for authorized lab/admin/project-lead users, wired to the delivery-kit API for save and reset actions. Unauthorized users see read-only item state.
- Added clear transfer-readiness gap copy listing incomplete delivery-kit items, plus static UI coverage for rendering, controls, API usage, and gap text.
- Verification: `npm test` (90 passing) and `npm run check` passed. In-app browser verification was blocked because the Browser plugin reported no available browser backends (`agent.browsers.list()` returned `[]`). A local demo-mode HTTP/API verification confirmed the workspace code was served, the assigned project lead could save a completed architecture item, `receiving-owner` received `403 FORBIDDEN`, and project payloads showed the completed item plus remaining gaps.
- Vercel preview: `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `app.js`, `styles.css`, `test/intake-draft-ui.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: surface transfer readiness as explicit missing structured items, not as a hidden gate count; authorized UI should call the same audited item API used by backend workflows.

## 2026-07-02 — US-033 Add Fellow assignment data model

- Added Fellow assignment records scoped to cycle and project, with fellow id, role, capacity units, manager id, status, manager acknowledgement, and outcome metadata.
- Added SQLite demo storage, tenant-scoped PostgreSQL migration/write paths, API endpoints for list/create/update/manager acknowledgement, and durable audit events for assignment creation, updates, and acknowledgement.
- Enforced state guards so assignments cannot become active until the recorded manager acknowledges them, and only the recorded manager can perform that acknowledgement.
- Verification: `npm test` (92 passing) and `npm run check` passed. `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `migrations/017_fellow_assignments.sql`, `server.mjs`, `src/fellow-assignments.mjs`, `src/labs-store.mjs`, `src/postgres-workflow-adapter.mjs`, `src/storage-port.mjs`, `src/workflow-service.mjs`, `package.json`, `test/migration-runner.test.mjs`, `test/postgres-workflow-adapter.test.mjs`, `test/workflow.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: keep Fellow capacity as governed assignment metadata and make manager acknowledgement the only path into active assignment state.

## 2026-07-02 — US-034 Build Fellow assignment UI

- Added a Fellow assignment workspace to the project brief with role, capacity, project, manager id, and manager acknowledgement status for each assignment.
- Added lab/admin assignment creation controls and manager-only acknowledgement actions. Empty and unavailable assignment states are announced with status text rather than disappearing.
- Added static UI coverage for assignment rendering, unavailable/empty states, create controls, acknowledgement controls, and API wiring.
- Verification: `npm test` (93 passing) and `npm run check` passed. In-app browser verification was blocked because the Browser plugin reported no available browser backends (`agent.browsers.list()` returned `[]`). A local demo-mode HTTP/API verification confirmed the workspace code was served, `lab-lead` could create an assignment, `submitter-1` received `403 FORBIDDEN`, and the recorded manager could acknowledge the assignment to `active`.
- Vercel preview: `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `app.js`, `styles.css`, `test/intake-draft-ui.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: Fellow assignment UI should reflect the server-owned manager acknowledgement state directly; do not imply capacity is active from assignment creation alone.

## 2026-07-02 — US-035 Require delivery readiness for transfer

- Updated decision gate evaluation so transfer readiness is derived from the required delivery-kit items. Incomplete items are returned as structured gate details such as `delivery_kit:architecture` and `delivery_kit:rollback`.
- Preserved the existing `delivery_kit` gate as an exception path and added a dedicated `delivery_kit_exception_accepted` audit event when that exception is accepted.
- Updated transfer tests to prove finalization is blocked until required delivery-kit items are completed, and added exception audit coverage.
- Verification: `npm test` (94 passing) and `npm run check` passed. `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `src/workflow-policy.mjs`, `src/workflow-service.mjs`, `src/postgres-workflow-adapter.mjs`, `src/postgres-read-adapter.mjs`, `src/labs-store.mjs`, `test/workflow.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: transfer readiness should be computed from required structured items; gate exceptions should be explicit audited risk acceptance, not a substitute for silently marking items complete.

## 2026-07-02 — US-036 Add approved artifact verification adapter

- Added an artifact verifier boundary that preserves HTTPS approved-origin checks and optionally validates provider records with fail-closed failure and timeout errors.
- Persisted verification status, verified timestamp, and method on evidence-backed gates, evidence entries, reviews, delivery-kit items, and handoffs across SQLite demo storage and PostgreSQL production adapters.
- Wired evidence-backed workflow mutations so failed artifact verification occurs before any gate-completing mutation or audit event is committed.
- Verification: `npm test` (100 passing) and `npm run check` passed. `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `migrations/018_artifact_verification_metadata.sql`, `src/artifact-verifier.mjs`, `src/labs-store.mjs`, `src/postgres-read-adapter.mjs`, `src/postgres-workflow-adapter.mjs`, `src/workflow-service.mjs`, `package.json`, `test/artifact-verifier.test.mjs`, `test/migration-runner.test.mjs`, `test/workflow.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: link allow-listing is necessary but not sufficient; workflow gates should only complete after the artifact adapter returns an explicit verified result.

## 2026-07-02 — US-037 Add work-tracking integration adapter

- Added a work-tracking adapter boundary for create/link and refresh operations with approved work-item URL validation, timeout handling, and fail-closed provider errors.
- Added a project work-item model storing provider, external reference, approved URL, external status, and last verified time across SQLite and tenant-scoped PostgreSQL paths.
- Added feature-gated create/link and refresh workflow/API mutations for project leads, lab leads, and admins, with durable audit events and no raw work-tracking payload storage.
- Verification: `npm test` (106 passing) and `npm run check` passed. `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `migrations/019_project_work_items.sql`, `server.mjs`, `src/labs-store.mjs`, `src/postgres-read-adapter.mjs`, `src/postgres-workflow-adapter.mjs`, `src/storage-port.mjs`, `src/work-tracking-adapter.mjs`, `src/workflow-service.mjs`, `package.json`, `test/migration-runner.test.mjs`, `test/postgres-read-adapter.test.mjs`, `test/postgres-workflow-adapter.test.mjs`, `test/work-tracking-adapter.test.mjs`, `test/workflow.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: external work state should be a verified reference and status snapshot, not a mirrored work-tracker record or issue payload.

## 2026-07-02 — US-038 Add calendar integration adapter

- Added a calendar adapter boundary for creating provider events or validating existing approved event links, with timeout and fail-closed provider errors.
- Added project calendar-event metadata for decision meetings and follow-ups, storing provider, external reference, approved URL, scheduled date, and last verification time only.
- Added feature-gated scheduling/list API and workflow mutations with state guards for open decision requests and accepted handoffs, plus durable audit events.
- Verification: `npm test` (112 passing) and `npm run check` passed. `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `migrations/020_project_calendar_events.sql`, `server.mjs`, `src/calendar-adapter.mjs`, `src/labs-store.mjs`, `src/postgres-read-adapter.mjs`, `src/postgres-workflow-adapter.mjs`, `src/storage-port.mjs`, `src/workflow-service.mjs`, `package.json`, `test/calendar-adapter.test.mjs`, `test/migration-runner.test.mjs`, `test/postgres-read-adapter.test.mjs`, `test/postgres-workflow-adapter.test.mjs`, `test/workflow.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: calendar integration state should be an approved event reference plus scheduled time; provider payloads and attendee details stay outside the command center.

## 2026-07-02 — US-039 Expose integration status in project workspace

- Added an integration status panel to the project brief showing artifact-link verification, work-tracking status, and calendar event verification with last verification times.
- Updated delivery-kit evidence copy so an unverified link is not presented as approved success, and added explicit warnings for artifact links that still need verification.
- Restricted provider/reference diagnostics for work and calendar integrations to lab lead and admin roles while leaving high-level verification state visible to ordinary authorized users.
- Verification: `npm test` (113 passing) and `npm run check` passed. Browser-control tooling was unavailable in this session; local demo-mode HTTP verification confirmed served integration markup, styles, and project payload metadata. `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `app.js`, `styles.css`, `test/intake-draft-ui.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: integration UI should distinguish verified references from merely present links, and provider identifiers are diagnostics rather than general workspace copy.

## 2026-07-02 — US-040 Add integration health reporting

- Added non-sensitive integration-attempt records for artifact, work-tracking, and calendar operations with operation, outcome, error code, project/entity metadata, actor id, and timestamp only.
- Added an administrator-only integration health endpoint and workflow method that summarize recent attempts and failures without exposing URLs, tokens, provider payloads, or raw error messages.
- Wired artifact verification, work-item create/link/refresh, and calendar scheduling paths to record success, failure, and timeout outcomes without masking the underlying workflow result.
- Verification: `npm test` (115 passing) and `npm run check` passed. `npx vercel env ls preview` reported `No Environment Variables found` for `chris-pigg-s-projects/dna-ai-labs-command-center`, so no preview was created because required production runtime configuration is absent.
- Files changed: `migrations/021_integration_attempts.sql`, `server.mjs`, `src/labs-store.mjs`, `src/postgres-workflow-adapter.mjs`, `src/storage-port.mjs`, `src/workflow-service.mjs`, `test/migration-runner.test.mjs`, `test/postgres-workflow-adapter.test.mjs`, `test/workflow.test.mjs`, `scripts/ralph/prd.json`, and `scripts/ralph/progress.txt`.
- Reusable learning: health records should make integration reliability diagnosable while keeping provider content and link targets out of durable operational telemetry.

---
